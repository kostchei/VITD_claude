# Godot 4 ----------------------------------------------------------------

# Godot imports generated assets here; rebuilt on import, never commit.
.godot/
# Legacy (Godot 3) import cache, just in case.
.import/

# Exported builds / packs
export.cfg
export_presets.cfg
*.pck
*.zip
build/
bin/

# Mono / .NET (this project uses the mono build; C# artifacts if ever added)
.mono/
data_*/
*.csproj
*.sln
obj/

# --- project-local tooling / scratch (not part of the game) ---
.tools/shots/

# OS / editor cruft -------------------------------------------------------
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store
*~
*.swp
.idea/
.vscode/
